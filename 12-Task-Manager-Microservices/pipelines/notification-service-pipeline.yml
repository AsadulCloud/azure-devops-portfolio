# notification-service pipeline - builds, pushes, and deploys ONLY notification-service.
# Triggers only when notification-service's code or its own manifest changes.

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - 12-Task-Manager-Microservices/notification-service/*
      - 12-Task-Manager-Microservices/k8s/notification-service.yaml

resources:
  - repo: self

variables:
  acrServiceConnection: 'acr-connection'
  azureSubscription: 'azure-subscription-connection'
  acrLoginServer: 'asadulcicdacr.azurecr.io'
  aksClusterName: 'asadulcicd-aks'
  aksResourceGroup: 'asadulcicd-rg'
  namespace: 'task-manager'
  imageTag: '$(Build.BuildId)'
  vmImageName: 'ubuntu-latest'

stages:

  - stage: Build
    displayName: 'Build and Push notification-service'
    jobs:
      - job: BuildAndPush
        pool:
          vmImage: $(vmImageName)
        steps:
          - task: Docker@2
            displayName: 'Build and push notification-service image'
            inputs:
              containerRegistry: '$(acrServiceConnection)'
              repository: 'task-manager-notification'
              command: 'buildAndPush'
              Dockerfile: '12-Task-Manager-Microservices/notification-service/Dockerfile'
              buildContext: '12-Task-Manager-Microservices/notification-service'
              tags: |
                $(imageTag)
                latest

  - stage: Deploy
    displayName: 'Deploy notification-service to AKS'
    dependsOn: Build
    condition: succeeded()
    jobs:
      - job: DeployToAKS
        pool:
          vmImage: $(vmImageName)
        steps:
          - task: AzureCLI@2
            displayName: 'Get AKS credentials'
            inputs:
              azureSubscription: '$(azureSubscription)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                az aks get-credentials \
                  --resource-group $(aksResourceGroup) \
                  --name $(aksClusterName) \
                  --overwrite-existing
                kubectl create namespace $(namespace) --dry-run=client -o yaml | kubectl apply -f -

          - task: Bash@3
            displayName: 'Replace image tag in manifest'
            inputs:
              targetType: 'inline'
              script: |
                cd 12-Task-Manager-Microservices/k8s
                sed -i "s|ACR_LOGIN_SERVER/task-manager-notification:latest|$(acrLoginServer)/task-manager-notification:$(imageTag)|g" notification-service.yaml
                cat notification-service.yaml

          - task: Kubernetes@1
            displayName: 'Apply notification-service manifest'
            inputs:
              connectionType: 'Azure Resource Manager'
              azureSubscriptionEndpoint: '$(azureSubscription)'
              azureResourceGroup: '$(aksResourceGroup)'
              kubernetesCluster: '$(aksClusterName)'
              namespace: '$(namespace)'
              command: 'apply'
              arguments: '-f 12-Task-Manager-Microservices/k8s/notification-service.yaml'
